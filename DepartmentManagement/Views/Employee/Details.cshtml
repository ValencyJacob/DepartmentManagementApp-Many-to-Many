@model Employee

<div class="container">

    <div class="card">
        <div class="card-header">
            @Model.FullName
        </div>
    </div>

    <a asp-action="Index" class="btn btn-primary">
        Back
    </a>

</div>  