@model Position

@{
    var title = "Добавить";
}

<br />
<br />
<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="Id" />
        @if (Model.Id != 0)
        {
            //edit
            title = "Редактировать";
        }

        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>

        <div class="col-8 pt-4">

            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="Name"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group row">

                <div class="col-4 offset-4">
                    @if (Model.Id == 0)
                    {
                        <button type="submit" class="btn btn-success form-control">Добавить</button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-success form-control">Редактировать</button>
                    }
                </div>

                <div class="col-4">
                    <a asp-action="Index" class="btn btn-warning form-control">Назад</a>
                </div>

            </div>
        </div>
    </div>
</form>

@section Scripts{
    <partial name="_ValidationScriptsPartial" />

}